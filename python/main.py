import argparse


if __name__ == '__main__':
    parser = argparse.ArgumentParser(description='Runs the maximum pseudo-likelihood \
     estimation \(MPLE) algorithm for stergm.')

    parser.add_argument('')